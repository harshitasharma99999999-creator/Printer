name: Universe Manifestation (All-in-One)

on:
  schedule:
    # Shorts + Instagram (primary growth loop)
    - cron: "0 */4 * * *"
    # Affiliate Marketing Shorts
    - cron: "0 5 * * *"
    - cron: "0 17 * * *"
    # Daily eBook + KDP print + long-form
    - cron: "30 1 * * *"
    # Spiritual Image Posts (Universe) — every 6 hours (offset from Shorts)
    - cron: "0 2,8,14,20 * * *"
  workflow_dispatch:
    inputs:
      run_shorts:
        description: "Run Shorts + Instagram"
        required: false
        default: "true"
      run_afm:
        description: "Run Affiliate Marketing (AFM) Shorts + Instagram"
        required: false
        default: "true"
      run_ebook_longform:
        description: "Run eBook publish + long-form YouTube"
        required: false
        default: "true"
      run_image_posts:
        description: "Run Spiritual Image Posts"
        required: false
        default: "true"

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

concurrency:
  group: universe-manifestation
  cancel-in-progress: true

jobs:
  shorts:
    name: Shorts + Instagram
    if: >
      github.event_name == 'workflow_dispatch' && inputs.run_shorts == 'true' ||
      github.event_name == 'schedule' && github.event.schedule == '0 */4 * * *'
    runs-on: ubuntu-latest
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install system dependencies
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y ffmpeg imagemagick libjpeg-dev zlib1g-dev libpng-dev

      - name: Install Python dependencies
        run: pip install -r requirements.txt

      - name: Create config.json
        run: |
          cat > config.json << 'EOF'
          {
            "verbose": true,
            "firefox_profile": "",
            "headless": true,
            "ollama_base_url": "http://127.0.0.1:11434",
            "ollama_model": "tinyllama",
            "groq_api_key": "",
            "groq_model": "llama-3.1-8b-instant",
            "nanobanana2_api_base_url": "https://generativelanguage.googleapis.com/v1beta",
            "nanobanana2_api_key": "${{ secrets.GEMINI_API_KEY }}",
            "nanobanana2_model": "gemini-2.5-flash-image",
            "nanobanana2_aspect_ratio": "9:16",
            "threads": 2,
            "zip_url": "",
            "is_for_kids": false,
            "google_maps_scraper": "",
            "email": {"smtp_server": "smtp.gmail.com", "smtp_port": 587, "username": "", "password": ""},
            "google_maps_scraper_niche": "",
            "scraper_timeout": 300,
            "outreach_message_subject": "",
            "outreach_message_body_file": "outreach_message.html",
            "stt_provider": "local_whisper",
            "whisper_model": "base",
            "whisper_device": "auto",
            "whisper_compute_type": "int8",
            "assembly_ai_api_key": "",
            "tts_voice": "Hugo",
            "font": "bold_font.ttf",
            "imagemagick_path": "/usr/bin/convert",
            "script_sentence_length": 5,
            "affiliate_link": "",
            "gumroad_access_token": "",
            "ebook_price": 4.99,
            "ebook_author": "Harshita Sharma",
            "kdp_firefox_profile": "",
            "gumroad_firefox_profile": "",
            "kdp_affiliate_link": "",
            "kdp_email": "",
            "kdp_password": ""
          }
          EOF

      - name: Seed .mp cache (Universe channel)
        run: |
          mkdir -p .mp
          cat > .mp/youtube.json << 'EOF'
          {"accounts": [{"id": "944ff01c-c1ee-44f4-a59f-cb3f44b056c0", "nickname": "universe", "firefox_profile": "", "niche": "manifestation law of attraction reality shifting raise your vibration", "language": "english", "videos": []}]}
          EOF

      - name: Run Shorts pipeline (Universe)
        env:
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
          YOUTUBE_TOKEN_JSON: ${{ secrets.YOUTUBE_TOKEN_JSON }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          # Keep topic aligned to the channel theme
          CONTENT_TOPIC: "manifestation law of attraction"
        run: python src/cron.py youtube 944ff01c-c1ee-44f4-a59f-cb3f44b056c0

      - name: Upload to Instagram Reels
        env:
          INSTAGRAM_USERNAME: ${{ secrets.INSTAGRAM_USERNAME }}
          INSTAGRAM_PASSWORD: ${{ secrets.INSTAGRAM_PASSWORD }}
          INSTAGRAM_SESSION_JSON: ${{ secrets.INSTAGRAM_SESSION_JSON }}
          INSTAGRAM_VIDEO_PATH: ".mp/last_short.mp4"
        run: python src/instagram_runner.py

      - name: Upload debug artifacts (on failure)
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: universe-shorts-debug
          path: |
            .mp/*.png
            .mp/*.mp4
            .mp/*.txt
          if-no-files-found: ignore

  afm:
    name: Affiliate Shorts + Instagram
    if: >
      github.event_name == 'workflow_dispatch' && inputs.run_afm == 'true' ||
      github.event_name == 'schedule' && (github.event.schedule == '0 5 * * *' || github.event.schedule == '0 17 * * *')
    runs-on: ubuntu-latest
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install system dependencies
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y ffmpeg imagemagick libjpeg-dev zlib1g-dev libpng-dev

      - name: Install Python dependencies
        run: pip install -r requirements.txt

      - name: Create config.json
        run: |
          cat > config.json << 'EOF'
          {
            "verbose": true,
            "firefox_profile": "",
            "headless": true,
            "ollama_base_url": "http://127.0.0.1:11434",
            "ollama_model": "tinyllama",
            "groq_api_key": "",
            "groq_model": "llama-3.1-8b-instant",
            "nanobanana2_api_base_url": "https://generativelanguage.googleapis.com/v1beta",
            "nanobanana2_api_key": "${{ secrets.GEMINI_API_KEY }}",
            "nanobanana2_model": "gemini-2.5-flash-image",
            "nanobanana2_aspect_ratio": "9:16",
            "threads": 2,
            "zip_url": "",
            "is_for_kids": false,
            "google_maps_scraper": "",
            "email": {"smtp_server": "smtp.gmail.com", "smtp_port": 587, "username": "", "password": ""},
            "google_maps_scraper_niche": "",
            "scraper_timeout": 300,
            "outreach_message_subject": "",
            "outreach_message_body_file": "outreach_message.html",
            "stt_provider": "local_whisper",
            "whisper_model": "base",
            "whisper_device": "auto",
            "whisper_compute_type": "int8",
            "assembly_ai_api_key": "",
            "tts_voice": "Hugo",
            "font": "bold_font.ttf",
            "imagemagick_path": "/usr/bin/convert",
            "script_sentence_length": 5,
            "affiliate_link": "https://www.amazon.in/s?k=manifestation+crystals+healing&tag=harshita000-21",
            "gumroad_access_token": "",
            "ebook_price": 4.99,
            "ebook_author": "Harshita Sharma",
            "kdp_firefox_profile": "",
            "gumroad_firefox_profile": "",
            "kdp_affiliate_link": "",
            "kdp_email": "",
            "kdp_password": ""
          }
          EOF

      - name: Seed .mp cache (Universe + AFM)
        run: |
          mkdir -p .mp
          cat > .mp/youtube.json << 'EOF'
          {"accounts": [{"id": "944ff01c-c1ee-44f4-a59f-cb3f44b056c0", "nickname": "universe", "firefox_profile": "", "niche": "manifestation law of attraction reality shifting raise your vibration", "language": "english", "videos": []}]}
          EOF
          cat > .mp/afm.json << 'EOF'
          {"products": [
            {"id": "afm-manifestation", "affiliate_link": "https://www.amazon.in/s?k=manifestation+crystals+healing&tag=harshita000-21"}
          ]}
          EOF

      - name: Run AFM pipeline
        env:
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          YOUTUBE_TOKEN_JSON: ${{ secrets.YOUTUBE_TOKEN_JSON }}
          AFFILIATE_LINK: "https://www.amazon.in/s?k=manifestation+crystals+healing&tag=harshita000-21"
        run: python src/cron.py afm afm-manifestation

      - name: Upload to Instagram Reels
        env:
          INSTAGRAM_USERNAME: ${{ secrets.INSTAGRAM_USERNAME }}
          INSTAGRAM_PASSWORD: ${{ secrets.INSTAGRAM_PASSWORD }}
          INSTAGRAM_SESSION_JSON: ${{ secrets.INSTAGRAM_SESSION_JSON }}
          INSTAGRAM_VIDEO_PATH: ".mp/last_short.mp4"
        run: python src/instagram_runner.py

      - name: Upload debug artifacts (on failure)
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: universe-afm-debug
          path: |
            .mp/*.png
            .mp/*.mp4
            .mp/*.txt
          if-no-files-found: ignore

  ebook_longform:
    name: eBook + KDP + Long-form
    if: >
      github.event_name == 'workflow_dispatch' && inputs.run_ebook_longform == 'true' ||
      github.event_name == 'schedule' && github.event.schedule == '30 1 * * *'
    runs-on: ubuntu-22.04
    timeout-minutes: 140
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install system dependencies
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y libjpeg-dev zlib1g-dev libpng-dev ffmpeg imagemagick
          sudo apt-get install -y google-chrome-stable || \
          (wget -q https://dl.google.com/linux/direct/google-chrome-stable_current_amd64.deb && \
           sudo apt install -y ./google-chrome-stable_current_amd64.deb) || true

      - name: Install Python dependencies
        run: pip install -r requirements.txt

      - name: Create config.json
        run: |
          cat > config.json << 'EOF'
          {
            "verbose": true,
            "firefox_profile": "",
            "headless": true,
            "ollama_base_url": "http://127.0.0.1:11434",
            "ollama_model": "tinyllama",
            "groq_api_key": "",
            "groq_model": "llama-3.1-8b-instant",
            "nanobanana2_api_base_url": "https://generativelanguage.googleapis.com/v1beta",
            "nanobanana2_api_key": "${{ secrets.GEMINI_API_KEY }}",
            "nanobanana2_model": "gemini-2.5-flash-image",
            "nanobanana2_aspect_ratio": "16:9",
            "threads": 2,
            "zip_url": "",
            "is_for_kids": false,
            "google_maps_scraper": "",
            "email": {"smtp_server": "smtp.gmail.com", "smtp_port": 587, "username": "", "password": ""},
            "google_maps_scraper_niche": "",
            "scraper_timeout": 300,
            "outreach_message_subject": "",
            "outreach_message_body_file": "outreach_message.html",
            "stt_provider": "local_whisper",
            "whisper_model": "base",
            "whisper_device": "auto",
            "whisper_compute_type": "int8",
            "assembly_ai_api_key": "",
            "tts_voice": "Hugo",
            "font": "bold_font.ttf",
            "imagemagick_path": "/usr/bin/convert",
            "script_sentence_length": 5,
            "affiliate_link": "",
            "gumroad_access_token": "",
            "ebook_price": 4.99,
            "ebook_author": "Harshita Sharma",
            "kdp_firefox_profile": "",
            "gumroad_firefox_profile": "",
            "kdp_affiliate_link": "https://www.amazon.in/s?k=manifestation+crystals+healing&tag=harshita000-21",
            "kdp_email": "",
            "kdp_password": ""
          }
          EOF

      - name: Create .mp cache directory + Universe channel seed
        run: |
          mkdir -p .mp
          cat > .mp/youtube.json << 'EOF'
          {"accounts": [{"id": "944ff01c-c1ee-44f4-a59f-cb3f44b056c0", "nickname": "universe", "firefox_profile": "", "niche": "manifestation law of attraction reality shifting raise your vibration", "language": "english", "videos": []}]}
          EOF

      - name: Run eBook pipeline (Universe Manifestation)
        env:
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
          GUMROAD_ACCESS_TOKEN: ${{ secrets.GUMROAD_ACCESS_TOKEN }}
          GUMROAD_SESSION_JSON: ${{ secrets.GUMROAD_SESSION_JSON }}
          GUMROAD_EMAIL: ${{ secrets.GUMROAD_EMAIL }}
          GUMROAD_PASSWORD: ${{ secrets.GUMROAD_PASSWORD }}
          KDP_EMAIL: ${{ secrets.KDP_EMAIL }}
          KDP_PASSWORD: ${{ secrets.KDP_PASSWORD }}
          KDP_TOTP_SECRET: ${{ secrets.KDP_TOTP_SECRET }}
          KDP_SESSION_JSON: ${{ secrets.KDP_SESSION_JSON }}
          KDP_PUBLISH_PAPERBACK: "true"
          KDP_PUBLISH_HARDCOVER: "true"
          REQUIRE_KDP_PUBLISH: "true"
          KDP_PRINT_TRIM_SIZE: "6x9"
          KDP_PRINT_PAPER: "cream"
          # Force topic so the content stays aligned with Universe Manifestation
          EBOOK_TOPIC: "manifestation and law of attraction"
        run: python src/cron.py ebook dummy-id groq

      - name: Export topic for long-form
        run: |
          if [ -f .mp/last_ebook_topic.txt ]; then
            echo "LONGFORM_NICHE=$(cat .mp/last_ebook_topic.txt)" >> $GITHUB_ENV
          else
            echo "LONGFORM_NICHE=manifestation and law of attraction" >> $GITHUB_ENV
          fi

      - name: Run long-form pipeline (Universe)
        env:
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
          YOUTUBE_TOKEN_JSON: ${{ secrets.YOUTUBE_TOKEN_JSON }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        run: python src/cron.py longform 944ff01c-c1ee-44f4-a59f-cb3f44b056c0 --niche "$LONGFORM_NICHE"

      - name: Upload debug artifacts (on failure)
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: universe-ebook-longform-debug
          path: |
            .mp/*.png
            .mp/*.pdf
            .mp/*.epub
            .mp/*.mp4
            .mp/*.txt
          if-no-files-found: ignore

  image_posts:
    name: Spiritual Image Posts
    if: >
      github.event_name == 'workflow_dispatch' && inputs.run_image_posts == 'true' ||
      github.event_name == 'schedule' && github.event.schedule == '0 2,8,14,20 * * *'
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install system dependencies
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y ffmpeg imagemagick libjpeg-dev zlib1g-dev libpng-dev

      - name: Install Python dependencies
        run: pip install -r requirements.txt

      - name: Create config.json
        run: |
          cat > config.json << 'EOF'
          {
            "verbose": true,
            "firefox_profile": "",
            "headless": true,
            "ollama_base_url": "http://127.0.0.1:11434",
            "ollama_model": "tinyllama",
            "groq_api_key": "",
            "groq_model": "llama-3.1-8b-instant",
            "nanobanana2_api_base_url": "https://generativelanguage.googleapis.com/v1beta",
            "nanobanana2_api_key": "${{ secrets.GEMINI_API_KEY }}",
            "nanobanana2_model": "gemini-2.5-flash-image",
            "nanobanana2_aspect_ratio": "9:16",
            "threads": 2, "zip_url": "", "is_for_kids": false,
            "google_maps_scraper": "", "google_maps_scraper_niche": "",
            "email": {"smtp_server":"smtp.gmail.com","smtp_port":587,"username":"","password":""},
            "scraper_timeout": 300, "outreach_message_subject": "",
            "outreach_message_body_file": "outreach_message.html",
            "stt_provider": "local_whisper", "whisper_model": "base",
            "whisper_device": "auto", "whisper_compute_type": "int8",
            "assembly_ai_api_key": "", "tts_voice": "Hugo",
            "font": "bold_font.ttf", "imagemagick_path": "/usr/bin/convert",
            "script_sentence_length": 5, "affiliate_link": "",
            "gumroad_access_token": "", "ebook_price": 4.99,
            "ebook_author": "Harshita Sharma", "kdp_firefox_profile": "",
            "gumroad_firefox_profile": "", "kdp_affiliate_link": "",
            "kdp_email": "", "kdp_password": ""
          }
          EOF

      - name: Create .mp cache and YouTube account seed
        run: |
          mkdir -p .mp
          cat > .mp/youtube.json << 'EOF'
          {"accounts": [{"id": "944ff01c-c1ee-44f4-a59f-cb3f44b056c0", "nickname": "universe", "firefox_profile": "", "niche": "reality shifting raise your vibration aura reading law of attraction manifesting 5D consciousness spiritual awakening", "language": "english", "videos": []}]}
          EOF

      - name: Run image post pipeline
        env:
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          INSTAGRAM_USERNAME: ${{ secrets.INSTAGRAM_USERNAME }}
          INSTAGRAM_PASSWORD: ${{ secrets.INSTAGRAM_PASSWORD }}
          INSTAGRAM_SESSION_JSON: ${{ secrets.INSTAGRAM_SESSION_JSON }}
          YOUTUBE_TOKEN_JSON: ${{ secrets.YOUTUBE_TOKEN_JSON }}
        run: python src/image_post_runner.py

      - name: Upload debug artifacts (on failure)
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: universe-image-posts-debug
          path: |
            .mp/*.png
            .mp/*.txt
          if-no-files-found: ignore
